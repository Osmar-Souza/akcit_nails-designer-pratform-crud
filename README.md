# Angular Project - Nails Designer Pratform

This is the Angular project nail salon management app.

## Setup

1. Install dependencies: `npm install`
2. Run the app: `npm start`

## Notes

- Charts are simplified placeholders; consider adding ngx-charts for full functionality.
- AI Assistant is a placeholder; needs implementation with Google GenAI.
- Uses Tailwind CSS for styling.